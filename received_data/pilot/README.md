# Pilot data

Data from the **pilot round** of the MCA Hunting Survey.

Put the CSV (and any JSON) exports the interviewers send back into this folder.
One file per phone/export is fine; they all share the same column layout and can
be stacked for analysis.

See the privacy note in [`../README.md`](../README.md) before committing files
that may contain respondent names — this repository is public.

_(This README is a placeholder so the folder exists in git; add the pilot files
alongside it.)_
